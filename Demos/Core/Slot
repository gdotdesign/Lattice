---JS
window.addEvent('domready', function(){
  group = new Core.Slot();
  for(i=0;i<5;i++){
    group.addItem(new Iterable.ListItem({title:i,removeable:false}));  
  }
  $$(".demo").grab(group);
});
